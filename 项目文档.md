# 恋爱日历小程序 - 项目完整文档

## 一、项目概述

情侣两人共同使用的微信小程序，功能包括：每日心情记录、日记、纪念日管理、照片上传、邀请码绑定。

## 二、关键账号信息

| 项目 | 信息 |
|------|------|
| 小程序 AppID | `wx58f20f88ad0c27c7` |
| 小程序密钥 | `ab4ff9cb9b3f29bcc14238b36aa19b22` |
| 云开发环境 ID | `cloudbase-d2gr49l7r2f948ed1` |
| 上传密钥文件 | `E:\平台文件\love-calendar\private.key` |
| 项目目录 | `E:\平台文件\love-calendar` |
| 最新版本号 | `2026.07.25.1100` |

## 三、技术架构

- **前端**：微信小程序原生框架（WXML + WXSS + JS）
- **后端**：前端直接操作云数据库 + `getOpenid` 云函数（获取 openid 和迁移数据）
- **数据库**：微信云开发文档型数据库（免费）
- **用户标识**：微信 openid（通过 `getOpenid` 云函数获取），同一微信号在所有设备上标识一致，自动跨设备同步
- **图片存储**：微信云存储（`wx.cloud.uploadFile`）
- **图片显示**：云存储 fileID（`cloud://` 开头）不能直接给 `<image>` 标签用，需要先调用 `wx.cloud.getTempFileURL()` 转换为临时链接

## 四、项目结构

```
E:\平台文件\love-calendar\
├── project.config.json          # 小程序项目配置（AppID 已填写）
├── private.key                  # 上传密钥
├── upload.js                    # 命令行上传脚本
├── package.json                 # npm 依赖（miniprogram-ci）
├── cloudbaserc.json             # CloudBase CLI 配置
├── generate-icons.js            # 图标生成脚本
├── README.md                    # 说明文档
├── 操作指南.md                   # 详细操作指南
├── 项目文档.md                   # 本文档
├── miniprogram/
│   ├── app.js                   # 入口：云开发初始化 + openid 获取 + 自动迁移
│   ├── app.json                 # 路由配置 + TabBar（8个页面）
│   ├── app.wxss                 # 全局样式（粉色主题 #FF6B81）
│   ├── sitemap.json
│   ├── utils/util.js            # 工具函数
│   ├── images/                  # TabBar 图标（8个PNG，40x40）
│   └── pages/
│       ├── welcome/             # 欢迎页（创建/加入空间）
│       ├── calendar/            # 日历页（心情记录+照片+在一起天数）
│       ├── diary/               # 日记列表页（时间线+缩略图）
│       ├── diary-edit/          # 日记编辑页（文字+最多9张图片）
│       ├── anniversary/         # 纪念日列表页（封面图+已过天数）
│       ├── anniversary-detail/  # 纪念日详情页（历史今日时间线）
│       ├── anniversary-record/  # 纪念日编辑/记录页（封面图+年份记录）
│       └── mine/                # 我的页面（头像+昵称修改+邀请码+解绑）
└── cloudfunctions/
    └── getOpenid/               # 获取 openid + 数据迁移（唯一使用的云函数）
```

## 五、数据库设计

### 环境信息
- 云开发环境 ID：`cloudbase-d2gr49l7r2f948ed1`
- 控制台地址：https://tcb.cloud.tencent.com
- 数据库类型：文档型数据库（非 MySQL/非 SQL 型）
- 所有集合权限：**「读取全部数据，修改本人数据」**

### 集合列表（全部 6 个，已创建）

| 集合名 | 说明 |
|--------|------|
| `couples` | 情侣配对，存储邀请码和双方 openid |
| `users` | 用户信息（昵称、头像、角色、openid） |
| `moods` | 每日心情记录（含照片） |
| `diaries` | 日记（含图片） |
| `anniversaries` | 纪念日（含封面图） |
| `anniversary_records` | 纪念日年度记录（历史今日，含照片） |

### 各集合字段结构

**couples**
```json
{
  "inviteCode": "X3K9M2",
  "creatorOpenid": "微信openid",
  "partnerOpenid": "微信openid",
  "createdAt": "ISO时间"
}
```

**users**
```json
{
  "openid": "微信openid（主要标识）",
  "uid": "旧版本UUID（迁移后保留，兼容旧数据）",
  "coupleId": "couples的_id",
  "nickname": "宝贝",
  "role": "creator|partner",
  "avatar": "cloud://文件ID",
  "migratedAt": "迁移时间（迁移后才有）",
  "createdAt": "ISO时间"
}
```

**moods**
```json
{
  "coupleId": "couples的_id",
  "uid": "记录者 openid（迁移后）",
  "date": "2026-07-24",
  "mood": "happy|sweet|calm|sad|angry",
  "note": "备注文字",
  "images": ["cloud://xxx.jpg"],
  "createdAt": "ISO时间"
}
```

**diaries**
```json
{
  "coupleId": "couples的_id",
  "uid": "作者 openid（迁移后）",
  "date": "2026-07-24",
  "title": "标题",
  "content": "内容",
  "images": ["cloud://xxx.jpg"],
  "createdAt": "ISO时间",
  "updatedAt": "ISO时间"
}
```

**anniversaries**
```json
{
  "coupleId": "couples的_id",
  "title": "在一起",
  "date": "2025-01-15",
  "type": "together|birthday|custom",
  "coverImage": "cloud://封面图",
  "createdBy": "openid（迁移后）",
  "createdAt": "ISO时间"
}
```

**anniversary_records**
```json
{
  "anniversaryId": "纪念日_id",
  "coupleId": "couples的_id",
  "uid": "记录者 openid（迁移后）",
  "year": 2026,
  "date": "2026-07-24",
  "content": "文字记录",
  "images": ["cloud://xxx.jpg"],
  "createdAt": "ISO时间"
}
```

## 六、核心功能清单

| 功能 | 说明 |
|------|------|
| 创建空间 | 输入昵称，生成 6 位邀请码 |
| 加入空间 | 输入邀请码 + 昵称绑定 |
| 自动跨设备同步 | 使用微信 openid，同一微信号在任何设备上数据一致 |
| 每日心情记录 | 5 种心情（😊😍😐😢😡）+ 文字 + 最多 9 张照片 |
| 月历展示 | 日历格子显示两人心情表情，有照片显示小圆点 |
| 日记 CRUD | 标题 + 文字 + 最多 9 张图片，时间线列表 |
| 纪念日管理 | 封面图 + 类型（在一起/生日/其他），显示已过天数 |
| 纪念日历史记录 | 可选择年份记录文字 + 照片，时间线回顾历史 |
| 头像修改 | 从相册/拍照上传 |
| 昵称修改 | 弹窗修改 |
| 邀请码分享 | 复制 + 微信分享卡片 |
| 解绑 | 创建者解绑清空全部数据 + 对方账号，加入者解绑只清自己 |
| 在一起天数 | 日历页顶部显示 |

## 七、openid 用户标识与数据迁移

### 用户标识机制

使用微信 openid 作为用户唯一标识。openid 通过 `getOpenid` 云函数的 `cloud.getWXContext().OPENID` 获取，同一微信号在所有设备上 openid 一致。

### 自动迁移流程

`app.js` 启动时执行以下流程：

1. 调用 `getOpenid` 云函数获取 openid
2. 调用 `check` 检查绑定状态：
   - 按 `openid` 查 `users` 表 → 找到 → 正常登录
   - 按旧 `uid` 查 `users` 表 → 找到 → 需要迁移
3. 迁移：调用 `migrate` 操作，将以下集合中所有 `uid` 字段从旧 UUID 替换为 openid：
   - `users`（添加 openid 字段）
   - `moods`
   - `diaries`
   - `anniversary_records`
   - `anniversaries`（`createdBy` 字段）
   - `couples`（`creatorUid`/`partnerUid`）

### 多设备使用

- 换手机：同一微信号登录，openid 不变，数据自动跟着走
- 电脑+手机：同一微信号，openid 一样，无需任何额外操作
- 清缓存：openid 不依赖本地存储，永远不会丢失

## 八、图片显示的重要机制

微信云存储的文件 ID 格式为 `cloud://环境ID.xxx/路径`，**不能直接**给 `<image>` 标签的 `src` 属性使用。

必须先用 `wx.cloud.getTempFileURL()` 转换为临时链接（有效期 2 小时），转换后的链接格式为 `https://` 开头的 URL。

**已在以下页面实现转换逻辑**：
- `anniversary/anniversary.js` — 纪念日列表页封面图
- `anniversary-detail/anniversary-detail.js` — 详情页封面图 + 历史记录图片
- `mine/mine.js` — 用户头像（自己和对方）

转换代码模式：
```javascript
// 筛选出 cloud:// 开头的 fileID
var fileIds = list.filter(function(item) {
  return item.coverImage && item.coverImage.indexOf('cloud://') === 0;
}).map(function(item) { return item.coverImage; });

if (fileIds.length > 0) {
  wx.cloud.getTempFileURL({ fileList: fileIds }).then(function(tempRes) {
    // 构建 fileID -> tempFileURL 映射
    var urlMap = {};
    tempRes.fileList.forEach(function(f) { urlMap[f.fileID] = f.tempFileURL; });
    // 替换原始数据
    list.forEach(function(item) {
      if (item.coverImage && urlMap[item.coverImage]) {
        item.coverImage = urlMap[item.coverImage];
      }
    });
    this.setData({ anniversaries: list });
  }.bind(this));
}
```

## 九、上传命令

```bash
# 进入项目目录
cd "E:/平台文件/love-calendar"

# 上传代码到微信后台
node upload.js --desc "版本描述"

# 生成预览二维码
node upload.js --preview
```

**IP 白名单问题**：公司网络 IP 是动态的，上传失败报 `invalid ip` 时，需要去 MP 后台添加当前 IP 到白名单。
- MP 后台路径：开发 → 开发管理 → 开发设置 → 小程序代码上传 → IP 白名单

## 十、MP 后台操作

### 小程序后台
- 地址：https://mp.weixin.qq.com
- 登录：微信扫码
- **设为体验版**：管理 → 版本管理 → 找到最新开发版本 → 点击该行右边「⋮」→ 选为体验版 → 手机扫码
- **代码上传密钥**：开发 → 开发管理 → 开发设置 → 小程序代码上传 → 生成密钥

### 云开发控制台
- 地址：https://tcb.cloud.tencent.com
- 登录：微信扫码，选择环境 `cloudbase-d2gr49l7r2f948ed1`
- **文档型数据库**：左侧菜单 → 文档型数据库（不是 SQL 型/MySQL）
- **添加集合**：点击「添加集合」→ 输入名称 → 确定
- **权限设置**：点击集合名 → 权限设置 → 选择「读取全部数据，修改本人数据」
- **云函数部署**：左侧菜单 → 云函数 → 找到 `getOpenid` → 上传部署

## 十一、当前状态

| 项目 | 状态 |
|------|------|
| 代码开发 | ✅ 完成（8 个页面，全部功能） |
| openid 改造 | ✅ 完成（UUID → openid） |
| 代码上传 | 待上传 |
| 云开发环境 | ✅ 已开通 |
| 数据库集合 | ✅ 6 个集合全部创建 |
| 云函数部署 | ⚠️ 需部署 `getOpenid` 云函数 |
| 体验版 | ⚠️ 需在 MP 后台更新为最新版本 |
| 测试 | 待测试 |
| 提交审核 | 待提交 |

## 十二、版本历史

| 版本 | 说明 |
|------|------|
| 2026.07.25.1100 | 用户标识改为微信 openid，自动跨设备同步，新增 getOpenid 云函数 |
| 2026.07.25.1030 | 纪念日记录支持选择年份，可录入历史年份记录 |
| 2026.07.25.1023 | 修复云存储图片显示（封面图、头像、历史记录照片） |
| 2026.07.25.1011 | 修复纪念日天数计算（统一显示已过天数） |
| 2026.07.25.1008 | 新增头像和昵称修改功能 |
| 2026.07.25.1003 | 纪念日增强：封面图 + 历史今日 |
| 2026.07.25.0938 | 无云函数版：全部改为直接操作数据库 |
| 2026.07.24.1843 | 配置云开发环境 ID |
| 更早版本 | 初始开发（云函数版本，已废弃） |

## 十三、重要注意事项

1. **IP 白名单**：公司网络 IP 是动态的，上传失败时需要去 MP 后台添加当前 IP
2. **数据库权限**：所有集合必须设为「读取全部数据，修改本人数据」，否则两人无法互看数据
3. **用户标识**：使用微信 openid（通过 `getOpenid` 云函数获取），同一微信号跨设备自动同步
4. **云函数 `getOpenid`**：必须先在云开发控制台部署，否则无法获取 openid
5. **数据迁移**：首次启动时自动将旧 UUID 数据迁移到 openid，迁移是幂等的（不会重复迁移）
6. **纪念日天数**：统一显示从纪念日到现在过了多少天，不再显示"天前/天后"
7. **图片存储**：所有图片上传到云存储，路径格式为 `类型/时间戳_随机数.jpg`
8. **图片显示**：云存储 fileID 必须先转临时链接再显示
9. **解绑逻辑**：创建者解绑会删除所有数据（所有集合 + 对方账号），加入者解绑只删除自己的记录
10. **数据库类型**：用文档型数据库，不是 MySQL/SQL 型
11. **兼容旧数据**：代码中 `userMap` 同时用 `openid` 和 `uid` 做 key，兼容迁移前后的数据

## 十四、会话恢复指南

继续开发时，请确认：
- 当前代码版本（最新 `2026.07.25.1100`）
- 数据库集合状态（6 个已创建）
- `getOpenid` 云函数是否已部署
- IP 白名单状态
- 当前需要修复的 bug 或新增功能

常用命令：
```bash
cd "E:/平台文件/love-calendar" && node upload.js --desc "描述"
```

## 十五、用户原始需求

1. 记录两个人的日常，包括每天心情、纪念日、日记
2. 支持上传日常照片
3. 纪念日可以上传封面图，每年纪念日能上传当年的记录
4. 支持"历史今日"回顾
5. 能修改昵称和头像
6. 个人申请的小程序，两个人用，免费方案
7. 不能下载微信开发者工具（公司网络限制），通过命令行上传
8. 腾讯云控制台部分页面加载不全（网络限制）
9. 纪念日只显示从那天到现在过了多少天，不需要倒计时