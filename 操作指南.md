# 💕 恋爱日历 — 从零到上线完整操作指南

---

## 📋 准备工作清单

在做任何操作之前，确认以下信息：

| 项目 | 状态 | 说明 |
|------|------|------|
| 小程序 AppID | ✅ `wx8274d82f9e0e7918` | 已配置到项目中 |
| 小程序名称 | `wxid_s2qpmnuanxf622的小程序测试号` | 注册完成 |
| 项目代码 | ✅ 已完成 | 位于 `E:\平台文件\love-calendar` |
| TabBar 图标 | ✅ 已生成 | 8 个 PNG 图标 |
| 微信开发者工具 | ⬜ 需要下载 | 见下方步骤 1 |

---

## 步骤 1：下载并安装微信开发者工具

1. 打开浏览器，访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 根据你的系统选择对应的版本下载（Windows 64位）
3. 下载完成后双击安装，一路「下一步」即可
4. 安装完成后打开，用你的微信扫码登录

---

## 步骤 2：导入项目

1. 打开微信开发者工具后，点击左侧的「导入项目」或右上角的「+」号
2. 填写项目信息：
   - **项目名称**：`恋爱日历`
   - **目录**：点击「选择」，浏览到 `E:\平台文件\love-calendar`
   - **AppID**：填入 `wx8274d82f9e0e7918`（应该会自动识别）
   - **开发模式**：选择「小程序」
   - **后端服务**：选择「微信云开发」
   - **模板**：选择「不使用模板」（重要！代码已经写好了）
3. 点击「确定」导入

---

## 步骤 3：开通云开发环境

1. 项目导入成功后，在开发者工具顶部工具栏中，点击「云开发」按钮（云朵图标）
2. 如果是第一次使用，会弹出开通页面
3. 点击「开通」按钮
4. 创建环境：
   - **环境名称**：输入 `love-calendar`（或任意名称）
   - **环境 ID**：系统会自动生成，例如 `love-calendar-xxxxx`
   - **付费模式**：选择「**免费版**」— 有 2GB 数据库 + 5GB 存储 + 5万次读写/月，完全够用
5. 点击「确定」创建

---

## 步骤 4：配置云开发环境 ID

1. 开通成功后，在云开发控制台页面的顶部，可以看到你的**环境 ID**（类似 `cloud1-xxxxx`）
2. 复制这个环境 ID
3. 回到代码编辑器，打开文件 `miniprogram/app.js`
4. 找到第 14 行：
   ```javascript
   env: '你的云开发环境ID',
   ```
5. 把它替换为你的实际环境 ID：
   ```javascript
   env: 'cloud1-xxxxx',
   ```
6. 按 `Ctrl+S` 保存

---

## 步骤 5：创建数据库集合

1. 在云开发控制台中，点击左侧菜单「数据库」
2. 点击「添加集合」按钮
3. 依次创建以下 5 个集合（**注意大小写，必须完全一致**）：

   | 集合名称 | 说明 |
   |---------|------|
   | `couples` | 情侣配对表 |
   | `users` | 用户表 |
   | `moods` | 心情记录表 |
   | `diaries` | 日记表 |
   | `anniversaries` | 纪念日表 |

4. 每个集合创建后，点击集合名称进入详情
5. 点击「权限设置」标签
6. 将权限改为「**仅创建者可读写**」
7. 对 5 个集合都做同样的权限设置

---

## 步骤 6：部署云函数

1. 在微信开发者工具左侧文件列表中，找到 `cloudfunctions` 目录
2. 展开后可以看到 5 个云函数文件夹：`auth`、`mood`、`diary`、`anniversary`、`user`
3. **对每个云函数**，右键点击文件夹 → 选择「**上传并部署：云端安装依赖**」
4. 部署顺序无所谓，等待每个部署完成（控制台会显示"上传成功"）
5. 全部 5 个云函数都部署完成后，可以到云开发控制台 → 「云函数」中确认，5 个函数都应该显示「部署成功」

---

## 步骤 7：预览测试

1. 点击开发者工具顶部工具栏的「预览」按钮
2. 会生成一个二维码
3. 用你的手机微信扫描二维码
4. 手机上将打开小程序，你应该看到欢迎页（粉色主题）

---

## 步骤 8：功能测试流程

### 8.1 测试创建空间
1. 在欢迎页，选择「创建空间」
2. 输入昵称（如"宝贝"）
3. 点击「💕 创建属于我们的空间」
4. 创建成功后，页面会显示 6 位邀请码，同时自动跳转到日历页

### 8.2 测试邀请码
1. 切换到「我的」Tab
2. 可以看到邀请码
3. 点击「复制」按钮，把邀请码发给对方（或发给自己另一个微信）

### 8.3 测试加入空间（用另一个微信）
1. 另一个微信打开小程序
2. 在欢迎页，选择「加入空间」
3. 输入昵称（如"老公"）和邀请码
4. 点击「💕 加入TA的空间」
5. 加入成功后自动跳转日历页

### 8.4 测试心情记录
1. 在日历页，点击今天的日期
2. 弹出心情选择器，选择一个心情（😊😍😐😢😡）
3. 可以写备注、添加照片
4. 点击「保存心情」
5. 日历格子上应该显示你选择的心情表情

### 8.5 测试日记
1. 切换到「日记」Tab
2. 点击右下角「+」按钮
3. 输入标题、内容，可以添加照片
4. 点击「发布日记」
5. 日记列表中应该显示刚发布的日记

### 8.6 测试纪念日
1. 切换到「纪念日」Tab
2. 点击右下角「+」按钮
3. 输入名称（如"在一起"）、选择日期、选择类型
4. 点击「添加纪念日」
5. 列表中应该显示纪念日卡片，带倒计时/天数

### 8.7 验证双方数据互通
1. 用另一个微信登录
2. 切换到日记 Tab，应该能看到对方写的日记
3. 切换到日历，应该能看到对方的心情记录
4. 切换到纪念日，应该能看到相同的纪念日列表

---

## 步骤 9：提交审核发布

1. 在微信开发者工具中，点击右上角「上传」按钮
2. 填写版本号（如 `1.0.0`）和备注（如"首次提交"）
3. 上传成功后，打开浏览器访问 https://mp.weixin.qq.com
4. 用你的微信扫码登录小程序管理后台
5. 左侧菜单找到「管理」→「版本管理」
6. 在「开发版本」中，找到刚上传的版本，点击「提交审核」
7. 填写审核信息：
   - **服务类目**：选择「工具」→「信息查询」或「工具」→「日程/日历」
   - **标签**：可以添加「恋爱」「日历」「日记」
8. 提交审核，个人小程序通常 **1-2 个工作日**审核通过
9. 审核通过后，点击「发布」即可上线

---

## 步骤 10：发布后的使用

上线后，任何人都可以通过以下方式使用：
- 微信搜索小程序名称
- 扫描小程序码
- 微信聊天中分享的小程序卡片

---

## 🔧 常见问题

### Q: 云开发开通失败？
A: 检查是否已实名认证。微信小程序需要完成实名认证才能开通云开发。

### Q: 云函数部署失败？
A: 右键云函数 → 选择「上传并部署：云端安装依赖（不上传 node_modules）」。如果还是失败，试试在云函数目录上右键 →「在终端中打开」，然后手动运行：
```bash
npm install --registry=https://registry.npmmirror.com
```
然后再右键 →「上传并部署」

### Q: 预览时页面空白？
A: 检查：
1. 云函数是否全部部署成功
2. 数据库集合是否全部创建
3. `app.js` 中的环境 ID 是否正确
4. 打开开发者工具的「调试器」查看 Console 是否有报错

### Q: 想修改主题色？
A: 全局搜索 `#FF6B81`（粉色）替换为你想要的颜色即可。主要在 `app.wxss` 和各页面的 `.wxss` 文件中。

### Q: 数据怎么备份？
A: 在云开发控制台 → 数据库 → 每个集合都可以「导出」为 JSON 文件。云存储中的图片也可以批量下载。

---

## 📊 项目文件总览

```
love-calendar/
├── miniprogram/                 # 37 个文件
│   ├── app.js                   # ✅ 入口文件
│   ├── app.json                 # ✅ 全局配置 + TabBar
│   ├── app.wxss                 # ✅ 全局样式（粉色主题）
│   ├── sitemap.json             # ✅ 站点地图
│   ├── pages/                   # 7 个页面（每个 3 个文件）
│   │   ├── welcome/             # ✅ 欢迎页
│   │   ├── calendar/            # ✅ 日历页（核心）
│   │   ├── diary/               # ✅ 日记列表
│   │   ├── diary-edit/          # ✅ 日记编辑
│   │   ├── anniversary/         # ✅ 纪念日列表
│   │   ├── anniversary-edit/    # ✅ 纪念日编辑
│   │   └── mine/                # ✅ 我的页面
│   ├── utils/util.js            # ✅ 工具函数
│   └── images/                  # ✅ 8 个 TabBar 图标
├── cloudfunctions/              # 5 个云函数（每个 2 个文件）
│   ├── auth/                    # ✅ 认证
│   ├── mood/                    # ✅ 心情
│   ├── diary/                   # ✅ 日记
│   ├── anniversary/             # ✅ 纪念日
│   └── user/                    # ✅ 用户
├── project.config.json          # ✅ 项目配置（已填 AppID）
└── README.md                    # ✅ 项目说明
```

---

## 🎉 一切就绪！

代码层面所有能做的事情已经完成：
- ✅ 7 个页面全部开发完成
- ✅ 5 个云函数代码就绪
- ✅ 8 个 TabBar 图标已生成
- ✅ AppID 已配置
- ✅ 粉色恋爱主题全局样式
- ✅ 图片上传功能（心情 + 日记都支持）
- ✅ 邀请码绑定流程
- ✅ 纪念日倒计时计算

**你只需要按上面步骤 1-9 操作即可！** 从下载开发者工具到上线发布，顺利的话一小时内可以完成。

有任何一步卡住了，随时告诉我，我帮你排查。💪